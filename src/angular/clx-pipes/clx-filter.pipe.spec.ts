import { ClxFilterPipe } from './clx-filter.pipe';

describe('ClxFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ClxFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
